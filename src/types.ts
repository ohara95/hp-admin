import firebase from "./config/firebese";

export type MenuData = {
  category: string;
  item: string;
  price: number;
};

export type BanquetData = {
  detail: string;
  price: number;
  title: string;
};
